package org.graphvis.engine

import org.graphvis.utils.{Edge, Node}

class PhysicsEngine(nodeList: Array[Node], edgeList: Array[Edge], confPath: String) {
  var nodes = nodeList
  var edges = edgeList
  // SpringSolver
  // RepulsionSolver

}
